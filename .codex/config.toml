approval_policy = "on-request"
sandbox_mode = "workspace-write"
web_search = "cached"

[features]
hooks = true
multi_agent = true

[agents]
max_threads = 6
max_depth = 1

[mcp_servers.sonarqube]
command = "sonar"
args = [ "run", "mcp", "--project", "DW225_ree-board" ]
